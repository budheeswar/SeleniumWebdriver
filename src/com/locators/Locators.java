package com.locators;

public class Locators {

	public static void main(String[] args) {
		// id , name , linkText, cssSelector, & xpath
		// cssSelector again have tag & Id, tag & class , tag & attribute, tag,class&attribute, innertext
		// absolute xpath, relative xpath
		//contains() , starts-with()
		DriverSetUp driverInitiator = new DriverSetUp();
		driverInitiator.initiateDriver();

	}

}
